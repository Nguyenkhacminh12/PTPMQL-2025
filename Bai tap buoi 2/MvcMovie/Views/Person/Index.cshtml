<h1>Dữ liệu từ Controller gửi về là: @ViewBag.Messeger</h1>

<div class="d-flex justify-content-center align-items-center vh-100 bg-light">
    <div class="card shadow-lg border-0 p-4" style="width: 100%; max-width: 500px; border-radius: 12px;">
        <div class="card-body">
            <h4 class="card-title text-center fw-bold mb-4 text-primary">📋 Nhập Thông Tin Người Dùng</h4>
            
            <form asp-action="Index" method="post" class="d-flex flex-column gap-3">
                <!-- PersonID -->
                <div>
                    <label class="form-label fw-semibold">🆔 Person ID:</label>
                    <input type="text" name="PersonID" class="form-control" placeholder="Nhập mã Person ID" required />
                </div>

                <!-- FullName -->
                <div>
                    <label class="form-label fw-semibold">👤 Họ tên:</label>
                    <input type="text" name="FullName" class="form-control" placeholder="Nhập họ tên đầy đủ" required />
                </div>

                <!-- Address -->
                <div>
                    <label class="form-label fw-semibold">🏠 Địa chỉ:</label>
                    <input type="text" name="Address" class="form-control" placeholder="Nhập địa chỉ của bạn" />
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-primary btn-lg w-100 mt-3" style="border-radius: 8px;">
                    🚀 Gửi dữ liệu
                </button>
            </form>
        </div>
    </div>
</div>
